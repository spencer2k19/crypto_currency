import 'package:flutter/material.dart';

class PriceSelection extends StatelessWidget {
  const PriceSelection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Price selection"),
    );
  }
}
